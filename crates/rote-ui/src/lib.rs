//! Retained-mode widget layer built on top of `rote-render`: panels,
//! status line, gutter, command palette. Deliberately minimal for now —
//! the goal is a small `Widget` trait and a handful of concrete widgets,
//! not a general-purpose UI framework.

pub struct Placeholder;
